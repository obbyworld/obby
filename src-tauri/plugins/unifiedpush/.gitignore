# Cargo lock for the local plugin lib — only apps should commit Cargo.lock,
# never libraries (https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html).
Cargo.lock

# Android Gradle build outputs (regenerated by `tauri android build`).
android/build/
